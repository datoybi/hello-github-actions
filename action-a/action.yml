name: "Hello Actions"
description: "Greet someone"
author: "dsy0302@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "Dasom"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
